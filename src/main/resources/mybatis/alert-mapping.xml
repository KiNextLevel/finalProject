<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AlertDAO">

    <select id="getAlertList" resultType="alert">
        SELECT ALERT_NUM, ALERT_CONTENT, ALERT_DATE, ALERT_ISWATCH
        FROM ALERT WHERE ALERT_MEMBER_EMAIL = #{userEmail}
                   ORDER BY ALERT_NUM DESC
    </select>

    <insert id="insert">
        INSERT INTO ALERT (ALERT_NUM, ALERT_MEMBER_EMAIL, ALERT_CONTENT, ALERT_DATE, ALERT_ISWATCH)
            VALUES (NVL((SELECT MAX(ALERT_NUM)+1 FROM ALERT), 1),#{userEmail}, #{alertContent}, CURRENT_TIMESTAMP, 0)
    </insert>

    <update id="update">
        UPDATE ALERT SET ALERT_ISWATCH = 1 WHERE ALERT_NUM = #{alertNumber}
    </update>
</mapper>